// TSS Output Script

// Every script needs a modifier function
const modifier = (text) => {
  text = onOutput_TSS(text);
 
  return { text }
}//end of modifier

// Don't modify this part
modifier(text)
